// $(document).on("submit", "#entries-form", handleEntriesFormSubmit);

//  // A function to handle what happens when the form is submitted to create a new Author
//  function handleEntriesFormSubmit(event) {
//     event.preventDefault();
//     // Don't do anything if a field hasn't been filled out
//     if (!word1Input.val().trim().trim()) {
//       return;
//     }
//     // Calling the upsertAuthor function and passing in the value of the name input
//     upsertEntries({
//       name: nameInput
//         .val()
//         .trim()
//     });

//     function upsertEntries (userEntries) {
//         $.post("/api/authors", userEntries)
//           .then(getAuthors);
//       }
//   }

  